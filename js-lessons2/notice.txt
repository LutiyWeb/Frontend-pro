1.Javascript сприймає строку як об'єкт який складаеться с символів, де кожен з них моє свій індекс.

2.String methods
//trim() - Прибирає пробіли з переду і з кінця строки.
//replaceAll() - приймаю два ргументи. Замінює перший на другий || replaceAll(' ', '.') - замінюємо всі пробіли на крапку
//replace() - прибирає перший пробій який знаходить
//replace() - прибирає перший пробій який знаходить
//indexOf() - приймає один аргумент у виді строки і перевіряє строку на співпадіння. Якщо знаходить повертає індекс на якому знайшов це співпадіння. Якщо не знаходить повертає -1
//slice() - приймає два аргументи. Індекси. Обрізає слово повертаючи нове. Обрізає слово від індексу вказанному у першому аргументі і до індексу вказанному у другому оргументу. Наприклад || 'Michel'.slice(0, 3) поверне 'Mic'